# ===========================================
# Production Requirements for EC2 AWS Ubuntu
# ===========================================
#
# CÁCH CÀI ĐẶT TRÊN EC2:
#
# 1. Cài system dependencies:
#    sudo apt-get update
#    sudo apt-get install -y libglib2.0-0 libsm6 libxext6 libxrender-dev
#
# 2. Tạo virtual environment:
#    python3 -m venv venv
#    source venv/bin/activate
#
# 3. Cài TẤT CẢ packages (1 lệnh duy nhất):
#    pip install -r requirements-production.txt
#
# ===========================================

# PyTorch CPU-only (tải từ PyTorch index)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision

# Web Framework
flask>=3.0.0
gunicorn>=21.0.0
werkzeug>=3.0.0

# Computer Vision (HEADLESS - không cần GUI)
opencv-python-headless>=4.8.0

# Data Processing
numpy>=1.24.0
pillow>=10.0.0

# Config
pyyaml>=6.0

# YOLO Model
ultralytics>=8.3.0

# HTTP & System
requests>=2.28.0
psutil>=5.9.0
packaging>=21.0
